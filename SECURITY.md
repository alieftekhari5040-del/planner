# سیاست امنیتی

## نسخه‌های پشتیبانی‌شده

در حال حاضر فقط آخرین نسخه‌ی شاخه‌ی پیش‌فرض و Pull Requestهای فعال پروژه بررسی امنیتی می‌شوند. این برنامه در حالت فعلی یک برنامه‌ی client-side است و داده‌ها را در `localStorage` مرورگر نگه می‌دارد.

## گزارش مسئولانه

اگر آسیب‌پذیری امنیتی پیدا کردید، آن را در Issue عمومی منتشر نکنید. از قابلیت **Private vulnerability reporting** در صفحه‌ی Security مخزن استفاده کنید یا از طریق کانال خصوصی GitHub با نگهدارنده تماس بگیرید.

گزارش بهتر است شامل موارد زیر باشد:

- شرح کوتاه و اثر احتمالی آسیب‌پذیری
- مراحل بازتولید یا نمونه‌ی حداقلی
- مرورگر، سیستم‌عامل و نسخه‌ی پروژه
- پیشنهاد اصلاح، در صورت وجود
- هر اطلاعاتی که برای بازتولید لازم است؛ بدون فایل پشتیبان یا داده‌ی شخصی واقعی

پس از دریافت گزارش، تلاش می‌کنیم در اولین فرصت آن را تأیید کنیم، شدت آن را ارزیابی کنیم و درباره‌ی اصلاح و انتشار اطلاع‌رسانی کنیم. لطفاً تا هماهنگی با نگهدارنده، جزئیات را عمومی نکنید.

## نکات امنیتی برای کاربران

- فایل‌های JSON پشتیبان می‌توانند شامل اطلاعات شخصی باشند؛ آن‌ها را در Issue، Pull Request یا مخزن عمومی بارگذاری نکنید.
- این برنامه احراز هویت و همگام‌سازی سمت سرور ندارد؛ روی دستگاه مشترک، داده‌های مرورگر را محافظت کنید.
- پیش از نصب نسخه‌های تغییرکرده، منبع و checksum انتشار را بررسی کنید.
