# راهنمای مشارکت

از علاقه‌ی شما به بهبود «برنامه‌ریز صعود» سپاسگزاریم. این سند مسیر استاندارد برای گزارش مشکل، پیشنهاد قابلیت و ارسال کد را توضیح می‌دهد.

## پیش از شروع

1. Issueهای باز و Pull Requestهای در حال بررسی را جست‌وجو کنید تا کار تکراری انجام نشود.
2. برای تغییرات بزرگ، ابتدا یک Issue با توضیح مسئله و راه‌حل پیشنهادی باز کنید.
3. تغییرات خود را کوچک، قابل بررسی و محدود به یک هدف نگه دارید.

## راه‌اندازی محیط

```bash
npm ci
npm run dev
```

پیش‌نیاز پروژه Node.js `20.19` یا جدیدتر است. پیش از ارسال تغییر، این فرمان را اجرا کنید:

```bash
npm run check
```

این فرمان به‌ترتیب lint، تست و build را اجرا می‌کند.

## استانداردهای کدنویسی

- از TypeScript دقیق و typeهای صریح استفاده کنید؛ از `any` جدید تا حد امکان پرهیز کنید.
- برای منطق قابل استفاده‌ی مجدد، تابع یا utility کوچک و قابل تست بسازید.
- متن‌های قابل مشاهده برای کاربر باید فارسی، خوانا و سازگار با راست‌به‌چپ باشند.
- برای تغییرات رابط کاربری، حالت موبایل، صفحه‌کلید، فوکوس، کنتراست و `prefers-reduced-motion` را بررسی کنید.
- اطلاعات شخصی را در log، fixture یا داده‌ی نمونه قرار ندهید.
- وابستگی جدید فقط وقتی اضافه شود که راه‌حل موجود کافی نباشد؛ دلیل آن را در توضیح Pull Request بنویسید.
- منطق تاریخ باید از utility تقویم شمسی استفاده کند و به فرض ثابتِ ۳۰ روز برای همه‌ی ماه‌ها متکی نباشد.
- نام‌گذاری فایل‌ها و کد به سبک موجود پروژه باشد؛ متن رابط کاربری فارسی و نام‌های فنی کد به انگلیسی استاندارد باقی بمانند.

## تست و بررسی تغییرات

برای هر قابلیت یا اصلاح باگ مناسب، تست واحد یا تست بازتولید اضافه کنید. در Pull Request موارد زیر را مشخص کنید:

- چه چیزی تغییر کرده است؟
- چگونه آن را بررسی کرده‌اید؟
- آیا رفتار ذخیره‌سازی محلی یا داده‌ی قدیمی تحت تأثیر قرار می‌گیرد؟
- اگر رابط کاربری تغییر کرده، تصویر یا توضیح قبل/بعد چیست؟

## روند Pull Request

1. از شاخه‌ی اصلی پروژه یک شاخه‌ی موضوعی بسازید؛ مانند `feat/jalali-import` یا `fix/calendar-leap-year`.
2. تغییرات را با commitهای کوچک و توصیفی ثبت کنید.
3. قالب Pull Request را کامل کنید.
4. مطمئن شوید `npm run check` بدون خطا اجرا می‌شود.
5. Pull Request را به شاخه‌ی پیش‌فرض پروژه باز کنید و به بازخوردهای بررسی پاسخ دهید.

برای پیام commit می‌توانید از الگوی زیر استفاده کنید:

```text
<type>: <توضیح کوتاه>
```

نوع‌های متداول: `feat`، `fix`، `docs`، `test`، `refactor`، `chore`.

## گزارش باگ

گزارش خوب شامل نسخه‌ی مرورگر و سیستم‌عامل، مراحل دقیق بازتولید، نتیجه‌ی مورد انتظار، نتیجه‌ی واقعی و در صورت امکان تصویر یا log مربوط است. اطلاعات شخصیِ موجود در پشتیبان JSON یا تصویر را پیش از ارسال حذف کنید.

## مجوز مشارکت

با ارسال مشارکت، تأیید می‌کنید که اثر شما تحت همان مجوز MIT پروژه منتشر شود و حق لازم برای انتشار آن را دارید.
